# Design: Self-Contained Capability Replay

Use one `MODIFIED Requirements` delta containing all seven exact requirement
titles. Integration can then start from the captured committed target and reach
the full 0.23 contract without relying on either prior uncommitted projection.
No runtime source or public artifact changes.
