/dist/
/node_modules/
*.tgz
*.tmp
.DS_Store

# Runtime data is external to the repository. These are ignored only as a
# defensive measure for third-party adapters.
ai/.runtime/
ai/.sdd-write-lock
ai/.phase-result.json

# Empirical uses committed state; only transient locks are ignored.
.empirical/*.lock

# Local secrets (Linear API key, etc.)
.env

# Coverage output is generated by the test gate.
coverage/
